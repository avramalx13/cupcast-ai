# CupCast AI Cleanup Report

Date: 2026-07-03

## Inventory

| Area | Count | Classification | Notes |
| --- | ---: | --- | --- |
| `src/cupcast/**/*.py` | 59 | active | Backend, prediction engine, simulator, analyst, mini-LLM, shared utilities. |
| `scripts/*.py` | 21 | active | CLI entrypoints for data validation, training, evaluation, simulation, docs, and API helpers. |
| `scripts/*.ps1` | 2 | active | Windows API/dev health helper scripts. |
| `tests/*.py` | 24 | active | Unit/integration coverage for backend, ML, reports, simulator, API, and exports. |
| `frontend/app/**/page.tsx` | 6 | active | Home, predictions, simulator, full tournament, analyst, and backtesting routes. |
| `frontend/components/*.tsx` | 10 | active | All components are imported by frontend routes or shell components. |
| `frontend/lib/*.ts` | 4 | active | API proxy client, teams loader, matchup helpers, and static demo loader. |
| `configs/*.yaml` | 6 | active | Prediction, real-data, mini-LLM, and simulation configs. |
| `data/raw/` | 4 files | active | Synthetic sample data and mini-LLM text fixtures. |
| `data/real/*.csv` | 3 files | manual_review/generated-local | User-supplied real CSVs. Kept and ignored by git. |
| `data/processed/` | 5 files + latest backtest job | generated | Real normalized matches, generated teams, snapshots, and latest API backtest job. |
| `models/` | 24 files | generated/active | Report artifacts, model cards, plots, and local model binaries used by docs/API/frontend export. |
| `frontend/public/reports/` | 10 files | generated/active | Static frontend mode inputs regenerated by `scripts/export_frontend_reports.py`. |
| `frontend/public/brand/` | 5 SVG + 5 PNG + README | active | Original CupCast visual assets used by the frontend. |
| `.github/workflows/ci.yml` | 1 | active | Runs pytest, readiness check, and frontend build. |
| `README.md`, `data/README.md`, `models/README.md` | 3 | active | Main portfolio and artifact documentation. |

## Files Removed

Removed only generated/runtime artifacts that are safe to recreate.

- `logs/*.log`: 18 local API/frontend/dev-start log files.
- `data/processed/backtest_jobs/*.json`: 72 stale API backtest job files from previous local runs.
- `data/processed/mini_llm_smoke/`: 3 stale smoke-test checkpoint files in the wrong generated-data location.
- `.pytest_cache/`: local pytest cache.
- `**/__pycache__/`: Python bytecode cache directories generated by compile/test runs.
- `frontend/public/brand/cupcast-26-emblem.svg`, `canada-character.svg`, `mexico-character.svg`, `usa-character.svg`: legacy host-themed brand assets replaced by the original CupCast AI brand set.

Kept the newest completed backtest job triplet:

- `data/processed/backtest_jobs/e72c6841-be3c-461e-a234-19993c8c1416.json`
- `data/processed/backtest_jobs/e72c6841-be3c-461e-a234-19993c8c1416_result.json`
- `data/processed/backtest_jobs/e72c6841-be3c-461e-a234-19993c8c1416_leaderboard.json`

## Files Modified

- `.gitignore`
  - Added `.DS_Store`.
  - Added recursive checkpoint ignores for `checkpoints/**/*.pt` and `checkpoints/**/*.pth`.
  - Added global `*.joblib` and `*.pkl` ignores.
  - Removed the ignore rule for `frontend/public/brand/*.svg` because the current original SVG brand assets are active frontend files and should remain eligible for source control.
- `scripts/check_project_readiness.py`
  - Removed a duplicate `scripts/simulate_full_tournament.py` entry from `REQUIRED_SCRIPTS`.
- `frontend/public/reports/*`
  - Regenerated static frontend report artifacts with `scripts/export_frontend_reports.py` so static mode matches current `models/` outputs.
- `docs/CLEANUP_REPORT.md`
  - Added this report.

## Kept Intentionally

- `data/real/results.csv`, `data/real/shootouts.csv`, `data/real/goalscorers.csv`
  - Required local real-data inputs. They remain ignored by `.gitignore`.
- `data/processed/real_completed_matches.csv`, `real_excluded_matches.csv`, `teams_real.csv`, `probability_snapshots.csv`
  - Generated artifacts used by the API, real-data mode, and simulation snapshots.
- `models/*.json`, `models/*.md`, `models/plots/*.png`
  - Active model evidence and portfolio docs. README, generated docs, and frontend report export depend on them.
- `models/prediction_model.joblib`, `models/prediction_model_real.joblib`
  - Local model binaries used by API/simulation/export paths. Ignored by git.
- `frontend/public/reports/*`
  - Active static frontend mode inputs. These should be regenerated, not deleted.
- `frontend/public/brand/*.svg`
  - Active original CupCast assets used by `BrandShowcase` and the app mark: `cupcast-logo.svg`, `cupcast-mark.svg`, `mascot-analyst.svg`, `mascot-striker.svg`, and `mascot-keeper.svg`.
- `frontend/public/brand/*-art.png`
  - Active user-provided CupCast artwork: `cupcast-header-mark-art.png` is used by the app header, and `mascot-analyst-art.png`, `mascot-striker-art.png`, and `mascot-keeper-art.png` are used by `BrandShowcase`. `cupcast-logo-art.png` remains available source artwork.
- `checkpoints/mini_llm_tiny/*.pt`
  - Local trained mini-LLM checkpoint artifacts. Ignored by git; kept because trained model files should not be removed unless clearly obsolete.
- `frontend/.next/` and `frontend/node_modules/`
  - Local generated/dependency folders. Ignored by git. `.next` was recreated by validation build.
- Synthetic demo data and static/API modes
  - Kept intentionally because tests, docs, and portfolio demo behavior depend on them.

## Candidate Files For Manual Review

- `checkpoints/mini_llm_tiny/*.pt`
  - Keep locally if you want the tiny analyst checkpoint for demos; delete locally only when you are sure it is no longer useful.
- `models/mini_llm_eval_report.json`
  - Active as an evaluation artifact, but it may be worth regenerating after any mini-LLM retrain.
- `data/processed/probability_snapshots.csv`
  - Runtime history used for probability timeline demos. Can grow over time; consider adding a snapshot pruning/export policy later.
- `data/real/goalscorers.csv`
  - Downloaded real file currently reserved for later player/scorer features. Keep as local ignored data; do not commit raw CSV.
- `Makefile` targets that rely on optional `data/real/elo_ratings.csv` and `data/real/fifa_rankings.csv`
  - Current code handles missing optional rating files gracefully, but the docs should stay clear that these files are optional.

## Findings

- No unused frontend components were found. Every component in `frontend/components/` is imported by a route or shell component.
- No safely removable Python modules or scripts were found. Scripts are referenced by README, Makefile, CI, tests, or other scripts.
- The broad generated-artifact problem was local runtime clutter, mainly stale backtest job JSON and logs.
- `.gitignore` was too broad for active brand SVGs and too narrow for nested checkpoints/global model binaries.
- `ruff` is not installed in the current environment, so static linting was not run.

## Validation Results

- `python -m compileall src scripts tests`: passed.
- `ruff`: unavailable (`No module named ruff`), skipped.
- `..\cupcast-mini-llm\.venv\Scripts\python.exe scripts\export_frontend_reports.py`: passed, warnings `[]`.
- `..\cupcast-mini-llm\.venv\Scripts\python.exe -m pytest`: passed, `87 passed, 13 warnings`.
- `..\cupcast-mini-llm\.venv\Scripts\python.exe scripts\check_project_readiness.py`: passed.
- `npm.cmd run build` in `frontend/`: passed.

## Risks And Notes

- The bare `D:\Tools\Python\python.exe` is not installed with the editable `cupcast` package, so direct `python scripts/export_frontend_reports.py` failed there. The shared workspace venv works and imports this project correctly.
- Pytest creates an API backtest job artifact through backend integration coverage. The cleanup kept the latest completed job and removed older job history.
- Real CSVs remain local and ignored. Do not commit `data/real/*.csv`.
- Generated model binaries and checkpoints remain local and ignored. Recreate them from scripts when preparing a clean clone.
